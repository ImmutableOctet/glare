#pragma once

#include <string>
#include <vector>

#include "Mesh.hpp"

namespace graphics
{
	class Context;
	class Canvas;

	class Model
	{
		//model(Context& ctx std::string path);
		//Model(Context& ctx, const std::vector<Mesh>& data);
	};
}