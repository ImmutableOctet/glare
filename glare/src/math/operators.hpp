#pragma once

#include "types.hpp"

// TODO: Revisit this header.

namespace math
{
	/*
	namespace operators
	{
		constexpr Vector3D operator/(const Vector3D& value, std::size_t divisor) // auto&&
		{
			auto divisor_f = static_cast<float>(divisor);

			return { (value.x / divisor_f), (value.y / divisor_f), (value.z / divisor_f) };
		}
	}

	// NOTE: May change this later.
	using namespace math::operators;
	*/
}